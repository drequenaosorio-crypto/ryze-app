import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.ryzeofficial.app',
  appName: 'Ryze',
  webDir: 'out',
  bundledWebRuntime: false
}

export default config
